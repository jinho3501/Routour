DATABASE_URL=postgresql+psycopg2://routour:routour@localhost:5432/routour
REDIS_URL=redis://localhost:6379/0

# Firebase Admin SDK 서비스 계정 JSON 파일 경로 (Firebase Console > 프로젝트 설정 > 서비스 계정에서 발급)
# 이 파일은 절대 git commit 하지 마세요
FIREBASE_SERVICE_ACCOUNT_PATH=./firebase-service-account.json
